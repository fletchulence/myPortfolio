import { alpha, styled } from '@mui/material/styles';
import { Button } from '@mui/material/Button';

const HeaderLinks = styled(Button)(({ theme }) => ({
   width: 20,
   color: theme.palette.secondary.main,


}));

export {
   HeaderLinks,

};

